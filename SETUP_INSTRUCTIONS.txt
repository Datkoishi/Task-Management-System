═══════════════════════════════════════════════════════════════
  HƯỚNG DẪN SETUP NHANH - TASK MANAGEMENT SYSTEM
═══════════════════════════════════════════════════════════════

⚠️  PostgreSQL chưa được cài đặt hoặc chưa chạy!

📦 CÁCH CÀI ĐẶT POSTGRESQL (Chọn 1 cách):

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CÁCH 1: Homebrew (Khuyến nghị - Nhanh nhất)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Cài đặt:
   brew install postgresql

2. Khởi động:
   brew services start postgresql

3. Tạo user root:
   psql postgres
   CREATE USER root WITH PASSWORD '18042005';
   ALTER USER root CREATEDB;
   \q

4. Setup:
   cd backend
   npm run init-db
   npm run create-admin
   npm run dev

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CÁCH 2: Docker (Nếu đã có Docker)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

docker run --name postgres-task \
  -e POSTGRES_USER=root \
  -e POSTGRES_PASSWORD=18042005 \
  -e POSTGRES_DB=task_management \
  -p 5432:5432 -d postgres:15

cd backend
npm run create-admin
npm run dev

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CÁCH 3: Postgres.app (GUI - Dễ dùng)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Tải: https://postgresapp.com/
2. Cài đặt và mở app, click "Initialize"
3. Chạy:
   /Applications/Postgres.app/Contents/Versions/latest/bin/psql postgres
   CREATE USER root WITH PASSWORD '18042005';
   ALTER USER root CREATEDB;
   \q

4. Setup:
   cd backend
   npm run init-db
   npm run create-admin
   npm run dev

═══════════════════════════════════════════════════════════════

📧 Thông tin đăng nhập Admin (sau khi setup):
   Email: admin@example.com
   Password: admin123

🌐 Frontend: http://localhost:3000
🔧 Backend: http://localhost:5000

═══════════════════════════════════════════════════════════════
